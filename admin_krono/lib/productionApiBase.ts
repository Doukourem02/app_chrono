/**
 * URL publique du backend en production (sans lecture d’env).
 * Référence unique pour éviter les divergences avec `next.config.ts` / CSP.
 */
export const PRODUCTION_API_BASE_URL = 'https://api.kro-no-delivery.com'
