diff --git a/node_modules/expo-widgets/ios/ExpoWidgets.podspec b/node_modules/expo-widgets/ios/ExpoWidgets.podspec
index 1d2b971..9783ee6 100644
--- a/node_modules/expo-widgets/ios/ExpoWidgets.podspec
+++ b/node_modules/expo-widgets/ios/ExpoWidgets.podspec
@@ -42,9 +42,11 @@ Pod::Spec.new do |s|
     :script => %Q{
       echo "Preparing ExpoWidgets.bundle..."
       source="#{__dir__}/../bundle/build/ExpoWidgets.bundle"
-      dest="${TARGET_BUILD_DIR}/${UNLOCALIZED_RESOURCES_FOLDER_PATH}/ExpoWidgets.bundle"
+      # expo/expo#43654 - JS runtime must land inside resource bundle embedded in .appex
+      dest="${TARGET_BUILD_DIR}/ExpoWidgets.bundle/ExpoWidgets.bundle"
       echo "Copying ${source} to ${dest}"
-      cp "${source}" "${dest}"
+      mkdir -p "$(dirname "${dest}")"
+      cp -R "${source}" "${dest}"
     },
     :execution_position => :before_compile,
     :input_files  => ["#{__dir__}/../bundle/build/ExpoWidgets.bundle"]
diff --git a/node_modules/expo-widgets/ios/Widgets/EntryView.swift b/node_modules/expo-widgets/ios/Widgets/EntryView.swift
index 947fc2e..c7ebb7e 100644
--- a/node_modules/expo-widgets/ios/Widgets/EntryView.swift
+++ b/node_modules/expo-widgets/ios/Widgets/EntryView.swift
@@ -31,7 +31,7 @@ public struct WidgetsEntryView: View {
     if let node {
       if #available(iOS 17.0, *) {
         WidgetsDynamicView(name: entry.name, kind: .widget, node: node, entryIndex: entry.entryIndex, environmentString: widgetEnvironmentString)
-          .containerBackground(.clear, for: .widget)
+          .containerBackground(Self.extractContainerBackground(from: node), for: .widget)
       } else {
         WidgetsDynamicView(name: entry.name, kind: .widget, node: node, entryIndex: entry.entryIndex, environmentString: widgetEnvironmentString)
       }
@@ -39,4 +39,34 @@ public struct WidgetsEntryView: View {
       EmptyView()
     }
   }
+
+  // expo/expo#43654 - containerBackground from root background modifier (iOS 17+)
+  private static func extractContainerBackground(from node: [String: Any]) -> Color {
+    guard
+      let props = node["props"] as? [String: Any],
+      let modifiers = props["modifiers"] as? [[String: Any]],
+      let bgModifier = modifiers.first(where: { ($0["$type"] as? String) == "background" }),
+      let hex = bgModifier["color"] as? String
+    else {
+      return Color.black
+    }
+    return Color(hex: hex) ?? Color.black
+  }
+}
+
+extension Color {
+  init?(hex: String) {
+    var hexString = hex.trimmingCharacters(in: .whitespacesAndNewlines)
+    if hexString.hasPrefix("#") {
+      hexString.removeFirst()
+    }
+    guard hexString.count == 6, let rgb = UInt64(hexString, radix: 16) else {
+      return nil
+    }
+    self.init(
+      red: Double((rgb >> 16) & 0xFF) / 255.0,
+      green: Double((rgb >> 8) & 0xFF) / 255.0,
+      blue: Double(rgb & 0xFF) / 255.0
+    )
+  }
 }
diff --git a/node_modules/expo-widgets/ios/Widgets/WidgetContext.swift b/node_modules/expo-widgets/ios/Widgets/WidgetContext.swift
index 68ff52a..ffed070 100644
--- a/node_modules/expo-widgets/ios/Widgets/WidgetContext.swift
+++ b/node_modules/expo-widgets/ios/Widgets/WidgetContext.swift
@@ -1,7 +1,14 @@
 import Foundation
 import JavaScriptCore
 
+// Cache JSContext per layout string. expo/expo#44543 - avoid recreation on each island refresh.
+private var cachedWidgetContexts: [String: JSContext] = [:]
+
 func createWidgetContext(layout: String) -> JSContext? {
+  if !layout.isEmpty, let cached = cachedWidgetContexts[layout] {
+    return cached
+  }
+
   guard let context = JSContext() else {
     return nil
   }
@@ -25,5 +32,9 @@ func createWidgetContext(layout: String) -> JSContext? {
   // Inject layout
   let layoutValue = context.evaluateScript("(\(layout))")
   context.setObject(layoutValue, forKeyedSubscript: "__expoWidgetLayout" as NSString)
+
+  if !layout.isEmpty {
+    cachedWidgetContexts[layout] = context
+  }
   return context
 }
diff --git a/node_modules/expo-widgets/scripts/xcode-build-bundle.sh b/node_modules/expo-widgets/scripts/xcode-build-bundle.sh
index 7dd91dc..13b5a27 100755
--- a/node_modules/expo-widgets/scripts/xcode-build-bundle.sh
+++ b/node_modules/expo-widgets/scripts/xcode-build-bundle.sh
@@ -4,15 +4,7 @@ set -eo pipefail
 
 EXPO_WIDGETS_PACKAGE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
 
-# For classic main project build phases integration, will be no-op to prevent duplicated bundle creation.
-#
-# `$PROJECT_DIR` is passed by Xcode as the directory to the xcodeproj file.
-# in classic main project setup it is something like /path/to/app/ios
-# in new style pod project setup it is something like /path/to/app/ios/Pods
-PROJECT_DIR_BASENAME=$(basename $PROJECT_DIR)
-if [ "x$PROJECT_DIR_BASENAME" != "xPods" ]; then
-  exit 0
-fi
+# expo/expo#43654 - do not exit when PROJECT_DIR is "ios" (expo run:ios); bundle would never build.
 
 # If PROJECT_ROOT is not specified, fallback to use Xcode PROJECT_DIR
 PROJECT_ROOT=${PROJECT_ROOT:-"$PROJECT_DIR/../.."}
